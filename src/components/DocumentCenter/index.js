import React, { Component, PropTypes } from 'react'
import { Icon,Row,Col,Button } from 'antd'


export default function DocumentCenter({ children }) {
  return (
		<div>

			{children}
			
		</div>
  )
}
